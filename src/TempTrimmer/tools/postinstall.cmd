@echo off
REM Kudu runs this script (from the NEW version's tools\ folder) after extension files are in place.
REM Nothing to do — HttpPlatformHandler starts TempTrimmer.exe automatically on the next request.
